{
    "nixEnvSelector.nixFile": "${workspaceRoot}/flake.nix",
    "nix.enableLanguageServer": true,
    "nix.serverPath": "nil",
    "nix.serverSettings": {
        "nil": {
            "formatting": {
                "command" : ["nix", "fmt"]
            }
        }
    }
}